const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(file => { file = path.join(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory()) { if (!file.includes('node_modules') && !file.includes('.next') && !file.includes('.git')) { results = results.concat(walk(file)); } } else { if (file.endsWith('.tsx') || file.endsWith('.ts')) { results.push(file); } } }); return results; } const files = walk('.'); let count = 0; const regex = /<div className=\"flex h-10 items-center justify-center rounded-xl bg-brand-emerald px-3 text-white shadow-sm(?:\s+hover:[^\"]*)?\">[\s\S]*?<\/div>/g; files.forEach(f => { let content = fs.readFileSync(f, 'utf8'); if (regex.test(content)) { content = content.replace(regex, '<img src=\"/logo.png\" alt=\"GoVendy\" className=\"h-9 w-auto object-contain\" />'); fs.writeFileSync(f, content); count++; } }); console.log('Replaced logo in ' + count + ' files');
